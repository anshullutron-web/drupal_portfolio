name: Get In Touch
description: Contact FAQ and form section

props:
  type: object
  properties:

    title:
      type: string

    description:
      type: string

    faqs:
      type: array
      items:
        type: object
        properties:
          question:
            type: string
          answer:
            type: string

    webform:
      type: string