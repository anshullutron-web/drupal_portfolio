{{ attach_library('portfoliolite/pricing') }}
<section class="pricing">
  <div class="container">
    <div class="pricing-header">
      <h2>{{ label }}</h2>
      {{ content.body }}
    </div>
    <div class="pricing-container">
      {{ content.field_pricing_pargraph }}
    </div>
  </div>
</section>